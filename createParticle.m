function folder = createParticle(windowSize, standardDev, delta, noiz, time)

%folder = particle class

folder.windowSize = windowSize;
folder.standardDev = standardDev;
folder.delta = delta;
folder.noiz = noiz;
folder.time = time;

end